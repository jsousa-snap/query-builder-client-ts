/**
 * Extension interface for subquery operations
 * Note: Most subquery functionality is covered by WhereExtensions and SelectExtensions
 */
export interface IQuerySubqueryExtensions<T> {
  // This interface is mainly for organization and to make it explicit
  // that this Queryable supports subquery operations
}
